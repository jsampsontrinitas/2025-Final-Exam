/**
 * Our page has the wrong heading, let's fix it! Instead of "Hello, Web!", we
 * want to say "Hello, Trinitas!"
 */
const h1 = document.querySelector('#mainHading');

h1.textContent = 'Hello, Trinitas!';
